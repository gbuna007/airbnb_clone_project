<%= simple_form_for(amenity, data: {
                              insert_in_list_target: 'form',
                              action: 'submit->insert-in-list#createAmenity'
                              }) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>
